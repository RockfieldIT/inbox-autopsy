EXPECTED PRIMARY CAUSE (do not show to the diagnostician): Failure mode #4 —
shared IP pool reputation. Authentication is fully aligned and passing
(dmarc=pass), the sender's own metrics are clean, but the platform's shared
SendGrid IP is Spamhaus CSS-listed and other customers of the same platform
are junked the same week. Decoys: the charity recently added ~800 new
addresses from a paper sign-up sheet (small, permissioned - not the cause);
donation-appeal wording (content red herring). A weak diagnostician will blame
the sender's own list or content; the correct call is the neighbourhood, not
the house.

